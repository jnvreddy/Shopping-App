<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Shopping" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary color -->
        <item name="colorPrimary">@color/primarybg</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primarybg</item>
    </style>

    <style name="Theme.Shopping" parent="Base.Theme.Shopping" />
    
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">

        <item name="android:windowBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Shopping</item>

    </style>

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name ="android:color">@color/primarybg</item>
    </style>

</resources>